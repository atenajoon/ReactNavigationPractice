// components:
// 1. Button

// two input fields: email, password
//  no validation, no DB

// onSubmit():
//  store user info in the user's redux (uID, email)
//  save user info in the Local Storage for later authentications

